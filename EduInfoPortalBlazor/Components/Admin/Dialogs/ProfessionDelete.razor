<MudDialog>
    <DialogContent>
        <MudTextField Value="@Profession.Name" Label="Название" ReadOnly="true" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Отменить</MudButton>
        <MudButton Color="Color.Info" OnClick="Delete">Удалить</MudButton>
    </DialogActions>
</MudDialog>

@code {

    [CascadingParameter]
    MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public Profession Profession { get; set; } = new Profession();

    void Cancel() => MudDialog.Cancel();

    void Delete() => MudDialog.Close(DialogResult.Ok(Profession));
}
